<%= simple_form_for @ticket do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :number %>
    <%= f.association :raffle %>
    <%= f.association :user %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, f.object.new_record? ? 'New Ticket' : 'Update Ticket', class: 'btn btn-primary btn-lg' %>
  </div>
<% end %>
