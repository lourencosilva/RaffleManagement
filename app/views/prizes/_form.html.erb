<%= simple_form_for prize do |f| %>

  <%= f.input :description%>
  <%= f.input :placing%>
  <%= f.association :user %>
  <%= f.association :ticket %>
  <%= f.button :submit, class: 'btn-primary'%>
<% end %>
